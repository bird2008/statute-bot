const { Client } = require('discord.js')

module.exports = {
    name: 'selfrole',
    description: "Zrobię to w najbliższym czasie",    

    run(msg, args){
        msg.reply("Zrobię to w najbliższym czasie")
    }
}   